name: "PR Security Gate"

on:
  pull_request:
    branches: [main, dev]

permissions:
  contents: read
  security-events: write

jobs:
  security-checks:
    name: "PR Checks"
    uses: ./.github/workflows/security-checks.yml
    secrets: inherit
